<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Meta tags remain unchanged -->
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/views/market/style.css">
   


</head>
 
 
<body style="background-color: #f0f2f5;">
 
<div class="container-fluid">
    <div class="row">
 
        <!-- Custom Sidebar -->
        <div class="sidebar">
            <!-- Sidebar content here -->
            <div>
              <a href="/"ß class="d-flex mb-3 mb-md-0 text-decoration-none">
                <span class="fs-4"><img src="/public/img/Logo-Instagram.png" style="width: 60%; margin-left: -2px; margin-bottom: 10px;"></span>
              </a>
             
              <ul class="nav">
                <li class="nav-item">
             
                  <a href="/user/feed" class="nav-link active" aria-current="page">
                     <img src="/public/img/homepage.png" alt="" style="width: 13%;">
                    <span class="aa">หน้าหลัก</span>
                  </a>

                   <a href="#" id="scan" class="nav-link link-body-emphasis" onclick="showPostPopup()">
                     <img src="/public/img/search.png" alt="" style="width: 12%;">
                     <span class="aa">ค้นหา </span>
                  </a>
               
               
                  <div id="scan_m" class="modal">
                    <div class="modal-content">
                
                        <label for="scan" class="form-label" style="margin-top: 10px;">ค้นหา</label>
                        <input type="text" class="form-control" id="scans" name="scans" placeholder="คำค้นหา...">
                        <div id="search-results-scan"  class="list-group"></div>
                    </div>
                </div>
                
                <script>
                    function showModal(modalId) {
                        var modal = document.getElementById(modalId);
                        modal.style.display = "block";
                        document.body.style.overflow = 'hidden';
                    }
                
                    function hideModal(modalId) {
                        var modal = document.getElementById(modalId);
                        modal.style.display = "none";
                        document.body.style.overflow = '';
                    }
                
                    document.getElementById('scan').addEventListener('click', function () {
                        showModal('scan_m');
                    });
                
                    // สคริปต์สำหรับการค้นหาที่อยู่
                    document.getElementById('scans').addEventListener('input', function () {
    var query = this.value;
    var resultsDiv = document.getElementById('search-results-scan');

    if (query.trim() !== '') {
        fetch('/search/api/scan?query=' + encodeURIComponent(query))
            .then(function (response) {
                if (!response.ok) {
                    throw new Error('Network response was not ok');
                }
                return response.json();
            })
            .then(function (data) {
                resultsDiv.innerHTML = '';
                data.results.forEach(function (result) {
                    var resultItem = document.createElement('a');
                    resultItem.href = "javascript:void(0);";
                    resultItem.className = 'list-group-item list-group-item-action';

                    // สร้างและเพิ่มภาพ
                    var image = document.createElement('img');
                    image.src = '/img/mk_img_mk/' + result.market_id;
                    image.alt = 'Image';
                    image.className = 'result-image';
                    resultItem.appendChild(image);

                    // สร้างและเพิ่มข้อความ (market_name)
                    var text = document.createElement('span');
                    text.className = 'result-text';
                    text.appendChild(document.createTextNode(result.market_name));
                    resultItem.appendChild(text);

                    // สร้างและเพิ่มข้อความ (mk_discript)
                    var descriptionText = document.createElement('span');
                    descriptionText.className = 'result-text2';
                    descriptionText.appendChild(document.createTextNode(result.business_name));
                    resultItem.appendChild(descriptionText);

                    resultItem.addEventListener('click', function () {
                        document.getElementById('scan').value = result.textContent;
                        resultsDiv.innerHTML = '';
                        window.location.href = '/market/show_mk?market_id=' + encodeURIComponent(result.market_id);
                    });

                    resultsDiv.appendChild(resultItem);
                });
            })
            .catch(function (error) {
                console.error('Fetch error:', error);
            });
    } else {
        resultsDiv.innerHTML = '';
    }
});

                </script>
                



               
               
                  <a href="#" class="nav-link">
                     <img src="/public/img/compass.png" alt="" style="width: 12%;">
                     <span class="aa">สำรวจ</span>
                  </a>
               
               
                  
                  <a href="#" id="postLink" class="nav-link link-body-emphasis" onclick="showPostPopup()">
                    <img src="/public/img/post.png" alt="" style="width: 12%; " >
                    <span class="aa">สร้าง</span>
                 </a>
 
   
 
                  <!-- Replace 'rounded-circle' with 'custom-rounded-img' -->
                     <a href="/market/profile" class="nav-link">
                    <img src="/img/file" class="custom-rounded-img" alt="" style="width: 25px; height: 25px; margin-right: 6px; margin-left: 3px;">
                        <span class="aa">โปรไฟล์</span>
                             </a>
 
 
                             <div class="dropdown">
                                   <button class="btn dropdown-toggle " type="button" >
                                       <img src="/public/img/more.png" alt="Logo" style="width: 25%; margin-right: 10px;">
                                       <span class="aa">เพิ่มเติม</span>
                                   </button>
                                   <ul class="dropdown-menu" style="display: none;">
                                       <li><a class="dropdown-item " href="/logout" >สลับบัญชี</a></li>
                                       <li><a class="dropdown-item " href="/logout">ออกจากระบบ</a></li>
                                   </ul>
                            </div>
   
                    </li>        
                    </div>
               
              </ul>
            </div>
          </div>
        </div>
        
       
                <div class="ads" style="margin-left:1550px; display: block; margin-top: 20px;height: 80vh;
                position:fixed;
                width: 250px; /* Adjusted width */
                overflow-x:hidden;
                overflow-y:hidden;
                z-index: 1000;
                
                padding: 20px;
                border-radius: 10px;
                "> 
                
                <div style="height: 200px;">
                
                    <div style="color: gray; margin-bottom: 10px; font-weight: 700;">บัญชีธุรกิจของคุณ</div>
                    <a href="/market/profile/business" style="text-decoration: none; color: inherit;">
                        <img src="/img/mk_img_mk/<%= market_id %>" style="width: 35px; height: 35px; object-fit: cover; border-radius: 50%; margin-top: 10px;">
                        <span style="margin-left: 10px; vertical-align: -5px; font-weight: 500;"><%= market_name %></span>
                    </a>
                    

                    <hr>

                    <div class="">
                        <% if(rows && rows.length) { %>
                        <table class="">
                            <div style="color: gray; margin-bottom: 10px; font-weight: 700;">บัญชีแนะนำ</div>
                            <tbody>
                                <% rows.forEach((row, index) => { %>
                                <tr>
                                    <td>
                                        <a href="javascript:void(0);" data-market-id="<%= row.market_id %>" class="profile-pic">
                                            <img src="/img/mk_img_mk/<%= row.market_id %>" style="width: 35px; height: 35px; object-fit: cover; border-radius: 50%; margin-top: 10px;">
                                            <span style="margin-left: 10px; vertical-align:-5px; font-weight: 500;"><%= row.market_name %></span>
                                        </a>
                                </td>
                                    
                                    
                                </tr>

                                <% }); %>
                            </tbody>
                        </table>
                        <% } else { %>
                        <p class="text-center">No User found!</p>
                        <% } %>
                    </div>
                
                </div>
            </div>
 



                <div class="main-content" style="width: 684px; margin: 0 auto;"> </div>


                <div id="postTypeModal" class="modal">
                    <div class="modal-content">
                        <span class="close">&times;</span>
                        <img src="/public/img/edit.png" alt="Edit">
                        <h2 style="text-align: center; font-size: 20px; margin-top: 20px;">Please Choose feeds or bords</h2>
                        <div class="modal-buttons-container">
                            <button id="newsFeedButton" style="margin-top: auto; margin-bottom: auto;">ฟีดข่าว</button>
                            <a href="your-link-here" class="button-link">สร้างกระดาน</a>
                        </div>  
                    </div>
                </div>
                
                
                
                <div id="newsFeedModal" class="modal">
                    <div class="modal-content">
                        <div class="modal-header">
                            <span class="close">&times;</span>
                            <h5 class="modal-title" style="font-weight: 700; font-size: 23px;">สร้างโพสต์</h5>
                        </div>
                        <div class="postHeader">
                            <div class="user-info">
                                <img src="/img/mk_img/<%= userId %>" alt="Profile Pic" class="profile-pic" style="object-fit: cover;">
                                <span style="font-weight: 600; font-size: 25px;"><%= market_name %></span>
                            </div>
                
                        </div>
                        <form id="newsFeedForm" class="postContent">
                            <textarea name="postContent" placeholder="คุณกำลังคิดอะไรอยู่ <%= market_name %>" maxlength="5000"></textarea>
                            <div id="imageUploadContainer" onclick="document.getElementById('fileInput').click()">
                                <p>คลิกเพื่อเลือกรูปภาพ</p>
                                <img id="previewImage" style="display:none;"/>
                                <button type="button" id="removeImageButton" style="display:none;" onclick="removeImage()">ลบรูปภาพ</button>
                            </div>
                            <input type="file" id="fileInput" name="postImage" style="display:none;" onchange="previewFile()">
                        </form>
                        <div class="postFooter">
                            <button type="submit" form="newsFeedForm" class="publish-button">เผยแพร่</button>
                        </div>
                    </div>
                </div>
                
                
                <script>
                    function removeImage() {
                    var preview = document.getElementById('previewImage');
                    var fileInput = document.getElementById('fileInput');
                    var removeButton = document.getElementById('removeImageButton');
                    preview.src = "";
                    preview.style.display = 'none';
                    removeButton.style.display = 'none';
                    fileInput.value = ""; // ล้างค่าของ input file
                }
                </script>
                <script>
                    function previewFile() {
                    var preview = document.getElementById('previewImage');
                    var file = document.getElementById('fileInput').files[0];
                    var reader = new FileReader();
                    var removeButton = document.getElementById('removeImageButton');
                    reader.onloadend = function () {
                        if (file) {
                            preview.src = reader.result;
                            preview.style.display = 'block';
                            removeButton.style.display = 'block'; // แสดงปุ่มลบ
                        } else {
                            preview.src = "";
                            preview.style.display = 'none';
                            removeButton.style.display = 'none'; // ซ่อนปุ่มลบ
                        }
                    }
                    if (file) {
                        reader.readAsDataURL(file);
                    }
                }
                </script>
                   
            <script>
                
                function showModal(modalId) {
                    var modal = document.getElementById(modalId);
                    modal.style.display = "block";
                    document.body.style.overflow = 'hidden';
                }
                // ซ่อน Modal ตาม ID
                function hideModal(modalId) {
                    var modal = document.getElementById(modalId);
                    modal.style.display = "none";
                    document.body.style.overflow = '';
                }
                // ฟังก์ชันเมื่อผู้ใช้กดปุ่ม "Post"
                document.getElementById('postLink').addEventListener('click', function() {
                    showModal('postTypeModal');
                });
                // ฟังก์ชันเมื่อผู้ใช้เลือก "ฟีดข่าว"
                document.getElementById('newsFeedButton').addEventListener('click', function() {
                    hideModal('postTypeModal');
                    showModal('newsFeedModal');
                });
                // จัดการปุ่มปิดบนทุก Modals
                var closeButtons = document.getElementsByClassName("close");
                for (var i = 0; i < closeButtons.length; i++) {
                    closeButtons[i].onclick = function() {
                        var modal = this.closest('.modal');
                        modal.style.display = "none";
                    }
                }
                // ปิด Modal เมื่อผู้ใช้คลิกนอกพื้นที่ Modal
                window.onclick = function(event) {
                    if (event.target.classList.contains('modal')) {
                        event.target.style.display = "none";
                    }
                }
                // จัดการการส่งข้อมูลฟอร์ม
                document.getElementById('newsFeedForm').addEventListener('submit', function(event) {
    // ป้องกันการส่งฟอร์มแบบปกติ
    

    // ดึงข้อมูลโพสต์จากฟอร์ม
    const postContent = document.querySelector('textarea[name="postContent"]').value;
    const postImage = document.getElementById('fileInput').files[0];

    // ตรวจสอบว่า postContent มีข้อมูลหรือไม่
    if (!postContent.trim()) {
        alert('โปรดใส่เนื้อหาของโพสต์ก่อนเผยแพร่');
        return; // หยุดการทำงานของฟังก์ชัน
    }

    // ตรงนี้เป็นโค้ดสำหรับส่งข้อมูลไปยังเซิร์ฟเวอร์
    const formData = new FormData();
    formData.append('postContent', postContent);
    formData.append('postImage', postImage);

    fetch('/post/api/post_Content', {
        method: 'POST',
        body: formData,
    })
    .then(response => response.text())
    .then(data => {
        console.log(data); // แสดงข้อมูลที่ได้รับจากเซิร์ฟเวอร์
    })
    .catch(error => {
        console.error('เกิดข้อผิดพลาด:', error);
        // สามารถจัดการข้อผิดพลาดได้ตามต้องการ
    });
});

                </script>

 





<script>
function toggleLike(postId, userId) {
    fetch(`/post/api/like?postId=${postId}&userId=${userId}`, { method: 'POST' })
        .then(response => response.json())
        .then(data => {
            let likeButton = document.querySelector(`#likeButton-${postId}`);
            let likeCount = document.querySelector(`#likeCount-${postId}`);

            if (data.liked) {
                likeButton.classList.add('liked');
                
             
            } else {
                likeButton.classList.remove('liked');
                
                
            }
            likeCount.textContent = `${data.likeCount} Likes`;
        })
        .catch(error => {
            console.error('Error:', error);
        });
}

            let currentPage = 0;
            const limit = 5;
            let isFetching = false;
 
    function loadMorePosts() {
    if (isFetching) return;
    isFetching = true;

    fetch(`/post/api/posts?page=${currentPage}&limit=${limit}`)
        .then(response => response.json())
        .then(data => {
            const mainContent = document.querySelector('.main-content');

            data.forEach(post => {
                const postElement = document.createElement('div');
                postElement.classList.add('post');
                postElement.style.marginTop = "20px";

                // กำหนดคลาส CSS และข้อความสำหรับปุ่มการถูกใจ
                let likeButtonClass = post.userLiked ? 'like-button liked' : 'like-button';
                let likeButtonText = post.userLiked ? '' : '';

                postElement.innerHTML = `

                                <div class="post" style="margin-top: 20px; position: relative;">
                    <div class="post-header" >
                        <img src="/img/select_b/${post.post_id}" alt="Profile Picture" class="profile-pic" style="object-fit:cover;" data-market-name="${post.market_id}" >
                        <div class="username" data-market-name="${post.market_id}" style="margin-top:-16px">${post.market_name}</div>
                        
                    <div class="cross" style="position: absolute; top: 0; right: 0; margin: 20px; padding-top: 10px; margin-top: -10px;">
                    <img src="/public/img/close.png" style="width: 25px;" class="hover-effect" onclick="toggleDropdown('${post.post_id}')">

                    <div class="dropdown-content" id="dropdown-content-${post.post_id}">
                     <a href="#" onclick="reportPost(${post.post_id});">รายงาน</a>
                    
                    </div>


                    </div>
    
                    </div>
                    <div class="a" style="margin-top:-22px; font-size:13px; margin-left:83px; color:gray;">${post.post_time}</div>

                    <div class="post-content">
                        ${post.post_discript}
                    </div>
                    <div class="post-image">
                        <img src="/img/select/${post.post_id}" alt="Post">
                    </div>
                    
                    <div class="post-footer">
                        <div id="post">
                            <div style="padding-left: 5px; padding-bottom: 7px;" id="likeCount-${post.post_id}">${post.likes} Likes</div>
                            <button class ="${likeButtonClass}" id="likeButton-${post.post_id}" onclick="toggleLike(${post.post_id},${post.user_id});">${likeButtonText}</button>
                    </div>
                </div>

                    
                `;
                mainContent.appendChild(postElement);
            });
            addUsernameClickListeners();

            currentPage++;
            isFetching = false;
        });
}

        window.onscroll = function() {
            if (window.innerHeight + window.pageYOffset >= document.body.offsetHeight - 10) {
        loadMorePosts();
         }
            };
                loadMorePosts();


                
            </script>


<script>
function toggleDropdown(postId) {
    const dropdownContent = document.querySelector(`#dropdown-content-${postId}`);
    const computedStyle = window.getComputedStyle(dropdownContent);

    if (computedStyle.getPropertyValue('display') === 'block') {
        dropdownContent.style.display = 'none';
        document.removeEventListener('click', closeDropdownOnClickOutside);
    } else {
        dropdownContent.style.display = 'block';

        // Use setTimeout to ensure the event listener is added after the UI update
        setTimeout(() => {
            document.addEventListener('click', closeDropdownOnClickOutside);
        }, 0);

        dropdownContent.addEventListener('mouseleave', function closeDropdown() {
            dropdownContent.style.display = 'none';
            dropdownContent.removeEventListener('mouseleave', closeDropdown);
            document.removeEventListener('click', closeDropdownOnClickOutside);
        });
    }

    function closeDropdownOnClickOutside(event) {
        if (!dropdownContent.contains(event.target)) {
            dropdownContent.style.display = 'none';
            document.removeEventListener('click', closeDropdownOnClickOutside);
        }
    }
}

             
function reportPost(postId) {
    // Prompt the user for a report reason
    let reportReason = prompt("กรุณาระบุเหตุผลในการรายงานโพสต์นี้:");
    if (reportReason !== null && reportReason.trim() !== "") {
        // Send the report request with the report reason
        fetch('/post/report-post/' + postId, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({ reason: reportReason }) // Send the report reason in the request body
        })
        .then(response => {
            if (response.ok) {
                console.log("โพสต์ถูกรายงานแล้ว");
                // You can update the UI or take any necessary actions
            } else {
                console.error("เกิดข้อผิดพลาดในการรายงานโพสต์");
            }
        })
        .catch(error => {
            console.error("Error:", error);
        });
    } else {
        // User cancelled the report or entered an empty reason
        console.log("การรายงานถูกยกเลิกหรือไม่มีเหตุผลที่ระบุ");
    }
}

</script>



            <script>
function addUsernameClickListeners() {
    const usernameElements = document.querySelectorAll('.profile-pic, .username');

    usernameElements.forEach(usernameElement => {
        if (!usernameElement.classList.contains('event-listener-added')) {
            usernameElement.addEventListener('click', function(event) {
                // ตรวจสอบว่า element มี attribute data-market-id หรือไม่
                const marketId = usernameElement.getAttribute('data-market-id') || usernameElement.getAttribute('data-market-name');
                const newURL = `/market/show_mk?market_id=${encodeURIComponent(marketId)}`;
                window.location.href = newURL;
            });
            usernameElement.classList.add('event-listener-added');
        }
    });
}

document.addEventListener('DOMContentLoaded', function() {
    addUsernameClickListeners();
});

            </script>



 
            <script>
 
document.addEventListener('DOMContentLoaded', function () {
    var dropdown = document.querySelector('.dropdown');
    var dropdownMenu = document.querySelector('.dropdown-menu');
 
    // เมื่อคลิกที่ปุ่ม dropdown
    dropdown.addEventListener('click', function (event) {
        // ป้องกันเหตุการณ์เริ่มต้นของลิงก์
       
       
        // แสดงหรือซ่อน dropdown menu
        if (dropdownMenu.style.display === 'block') {
            dropdownMenu.style.display = 'none';
        } else {
            dropdownMenu.style.display = 'block';
        }
    });
 
    // ซ่อน dropdown เมื่อคลิกนอกเขต dropdown
    document.addEventListener('click', function (event) {
        var isClickInside = dropdown.contains(event.target);
 
        if (!isClickInside) {
            dropdownMenu.style.display = 'none';
        }
    });
});
 
       </script>
 

 
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.1.16/dist/umd/popper.min.js"></script>
<script src="script.js"></script>




 
</body>
</html>